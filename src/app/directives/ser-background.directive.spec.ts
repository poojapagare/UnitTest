import { SerBackgroundDirective } from './ser-background.directive';

describe('SerBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new SerBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
