import { IfconditionDirective } from './ifcondition.directive';

describe('IfconditionDirective', () => {
  it('should create an instance', () => {
    const directive = new IfconditionDirective();
    expect(directive).toBeTruthy();
  });
});
