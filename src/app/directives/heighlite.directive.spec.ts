import { HeighliteDirective } from './heighlite.directive';

describe('HeighliteDirective', () => {
  it('should create an instance', () => {
    const directive = new HeighliteDirective();
    expect(directive).toBeTruthy();
  });
});
